{
  "permissions": {
    "allow": [
      "Bash(rtk ls *)",
      "Bash(.venv/Scripts/python -m pip install pytest httpx --quiet)",
      "Bash(VIRTUAL_ENV=\".venv\" rtk uv pip install httpx)",
      "Bash(.venv/Scripts/python -m pytest tests/ -v --tb=short)",
      "Bash(rtk uv *)",
      "Bash(VIRTUAL_ENV=\"$\\(pwd\\)/.venv\" rtk uv pip install -r requirements-test.txt)",
      "Bash(rtk grep *)"
    ]
  }
}
